services:
  postgres:
    image: postgres:18
    container_name: valute_postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: valute
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql
 
volumes:
  pg_data:

